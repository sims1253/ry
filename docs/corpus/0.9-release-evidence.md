# 0.9 release evidence

Final reconciled evidence for ry 0.9.0. Every metric below traces to a
committed gate — a test, a ledger, or a corpus report — not to a planning
document.

## Corpus provenance

| Field | Value |
| :-- | :-- |
| Ledger | `docs/corpus/posit-0.9.0.json` |
| Corpus | Posit open-source (62 packages, ≥500 stars) |
| Reconciliation | hermetic (`RY_NO_INSTALLED_LIBRARIES=1`) |
| Source reports | `ecosystem/reports/posit.*.root.txt` |
| Source SHA-256 | `602fe749c12cfb2217d96fdb82a93634907a4f1d4f9507a27abb51d5209fb7e7` |
| ry commit | `ec702b587f2ab5a78f64182654d2b1865b44adb0` (Plan 35 head) |
| Full-tier check | `ecosystem/run.sh --check --tier full` — 728 identities match |
| Drift detection | `ecosystem/test-drift-detection.sh` — PASS |
| Label falsification | `ecosystem/test-posit-label-falsification.sh` — PASS |

## Final precision and TP retention

| Metric | Value |
| :-- | --: |
| Total findings | 728 |
| True positives | 37 |
| False positives | 691 |
| Uncertain | 0 |
| Overall precision | 5.08% |
| `R/` findings | 547 |
| `R/` TP / FP | 23 / 524 |
| `R/` precision | 4.20% |
| Original 0.8.0 TP | 34 |
| Surviving original TP | 31 |
| TP retention | 91.18% |
| New TP (Plan 34 audit) | 6 |
| Packages with ≥1 diagnostic | 39 / 62 |

The 3 resolved original TP are intentional bad-code fixtures in lintr and
styler test suites — not production-source defects. All 31 surviving original
TP are unchanged. The 6 new TP were individually reviewed at source in
Plan 34-W1.

**Gate:** `ecosystem/run.sh --check --manifest ecosystem/posit-packages.txt
--ledger docs/corpus/posit-0.9.0.json --tier full`.

## Rule verdict counts

| Rule | Total | TP | FP |
| :-- | --: | --: | --: |
| RY000 | 6 | 2 | 4 |
| RY001 | 29 | 6 | 23 |
| RY002 | 3 | 0 | 3 |
| RY010 | 476 | 4 | 472 |
| RY030 | 25 | 0 | 25 |
| RY031 | 2 | 0 | 2 |
| RY032 | 48 | 1 | 47 |
| RY033 | 41 | 6 | 35 |
| RY034 | 3 | 3 | 0 |
| RY040 | 23 | 0 | 23 |
| RY060 | 5 | 0 | 5 |
| RY061 | 21 | 0 | 21 |
| RY070 | 8 | 2 | 6 |
| RY080 | 2 | 0 | 2 |
| RY090 | 4 | 0 | 4 |
| RY091 | 5 | 1 | 4 |
| RY092 | 3 | 0 | 3 |
| RY093 | 4 | 4 | 0 |
| RY098 | 1 | 0 | 1 |
| RY100 | 4 | 4 | 0 |
| RY102 | 1 | 1 | 0 |
| RY103 | 2 | 2 | 0 |
| RY105 | 12 | 1 | 11 |

Per-rule semantic verdicts (R7 literal-lift, mutation pilot) are committed in
`docs/corpus/rule-evidence-0.9.md`.

**Gates:** `crates/ry-checker/tests/oracle.rs` (completeness),
`crates/ry-checker/tests/rule_evidence.rs` (mutation pilot),
`crates/ry-checker/tests/probes.rs` (probe coverage).

## Cross-mode matrix status

The CLI (`ry check`) and LSP server publish identical diagnostics on the same
workspace. This is verified by deterministic per-file differential tests:

| Contract | Test | Status |
| :-- | :-- | :-- |
| Single-root file eligibility | `protocol.rs::cli_and_run_with_publish_the_same_single_root_matrix` | PASS |
| Per-folder settings (#44) | `p36_contract.rs::p36_w2a_two_roots_different_editor_settings_differential` | PASS |
| Colliding local stubs (#54) | `p36_contract.rs::p36_w2b_colliding_local_stubs_isolation` | PASS |
| Per-folder custom config (#56) | `p36_contract.rs::p36_w2c_per_folder_custom_config_path` | PASS |
| Discovery-set equality (#48) | `p36_contract.rs::p36_w7_cli_lsp_discovery_set_equality` | PASS |
| Baseline reload (#45) | `p36_contract.rs::p36_w5_baseline_reload_converges_to_new_value` | PASS |
| Publish-path no disk I/O (#45) | `p36_contract.rs::p36_w5_publish_path_performs_no_baseline_disk_io` | PASS |

## LSP session seed count

| Lane | Seeds | Operations per seed | Status |
| :-- | --: | --: | :-- |
| PR (`w8_pr_session_seeds`) | 16 | 1–8 | PASS |
| Nightly (`w8_nightly_session_seeds`) | 1000 | 1–8 | PASS (550 s) |

Both lanes use fixed deterministic ChaCha seeds — no random proptest RNG.
Proptest shrinking is preserved through `TestRunner::new_with_rng`.

**Gate:** `cargo test -p ry-lsp --test w8_session` (PR);
`cargo test -p ry-lsp --test w8_session -- --ignored w8_nightly_session_seeds`
(nightly).

## Cap behavior (discovery bounds)

| Cap | Default | `ry.toml` key | Visible |
| :-- | --: | :-- | :-- |
| files per root | 20,000 | `index.max-files` | tracing event + LSP warning |
| bytes per R file | 2 MiB | `index.max-file-bytes` | tracing event + LSP warning |
| directory depth | 64 | `index.max-depth` | tracing event + LSP warning |

Caps are configurable in `ry.toml` and visible when hit. Zero is a
configuration error, not an undocumented "unlimited" sentinel. CLI and LSP
use the same shared `ry-workspace` discovery module.

**Gate:** `p36_contract.rs::p36_w7_cli_lsp_discovery_set_equality`;
`w8_session.rs::w8_catches_issue_48_discovery_caps`.

## P36 historical bug coverage

Every P36 historical bug is caught by a deterministic case in the W8
state-machine property:

| Issue | Bug | Catching test |
| :-- | :-- | :-- |
| #44 | Editor settings server-wide | `w8_catches_config_reload` |
| #45 | Baseline I/O under lock | `w8_catches_issue_45_baseline_reload` |
| #48 | Indexer unbounded | `w8_catches_issue_48_discovery_caps` |
| #53 | Tree cache no version stamp | `w8_catches_issue_53_stale_parse` |
| #55 | Folder changes don't refresh index | `w8_catches_issue_55_folder_mutation` |
| #56 | Configuration override dead | `w8_catches_config_reload` |

The injected stale-state defect (model diverges after a config edit) is
shrunk by proptest to 3 operations — well under the 10-operation bound.

**Gate:** `w8_session.rs::w8_property_shrinks_injected_defect_to_under_ten_ops`.

## Remaining known gaps

1. **Warm-start cache**: explicitly deferred. The unwired draft
   (`crates/ry-checker/src/cache.rs`) round-tripped only `known_vars`,
   `callable_vars`, and `RType::Unknown`; it has since been deleted (see
   `docs/architecture/cache-decision.md`). A future plan must specify a
   complete `CollectedFile` round trip before wiring a cache.

2. **Precision**: 5.08% overall (4.20% `R/`). The dominant FP source is RY010
   (476 FP — imported/generated/data bindings that exist at runtime). This
   is a design decision (no runtime binding resolution), not a regression.

3. **RY032 parameter-vector heuristic**: 48 FP. P34-W2 policy decided that
   unknown parameter length is not evidence that `&&`/`||` discards elements.
   The R7 literal-lift report confirms RY032 as param-unreachable.

4. **Metadata edits in the random W8 alphabet**: NAMESPACE/DESCRIPTION/typeshed
   edits are exercised by a dedicated convergence test
   (`w8_metadata_edits_converge`) rather than the random proptest alphabet,
   because they require package-structured fixtures. The random alphabet
   covers on-disk file lifecycle, config, baseline, discovery caps, parse
   races, workspace-folder mutation, and server restart.

5. **LSP session timing**: the 1000-seed nightly property uses a 60 ms
   drain idle-timeout to detect end-of-stream after the quiescence
   publication. This is a bounded drain, not a sleep for computation.
