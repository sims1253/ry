---
source: crates/ry-checker/tests/vendor_snapshot.rs
expression: rendered
---
- "utils.R:32:7 RY107 `any()` returns a length-1 logical unless an `any`/`all` or `Summary`-group method dispatches, so this comparison is TRUE exactly when the call is FALSE (`NA` when it is `NA`); the comparison was probably meant for the elements: `any(lengths == 0)`"
