[package]
name = "ry-analysis"
description = "Unified diagnostics query for ry — parsed files and workspace context in, diagnostics out."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
ry-core = { path = "../ry-core" }
ry-checker = { path = "../ry-checker" }
ry-workspace = { path = "../ry-workspace" }
ry-typeshed = { path = "../ry-typeshed" }
