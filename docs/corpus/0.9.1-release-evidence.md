# 0.9.1 stabilization

The [release tracker](https://github.com/sims1253/ry/issues/392) records the
candidate commit, CI runs, artifact checksums, and installed-product checks.
This release keeps the six existing core targets and the Rust 1.88 minimum.
The VS Code and Positron extension version is 0.9.2 and bundles core 0.9.1.
Zed's gallery version remains independent; its automatic download selects
the latest stable core release.

## Accepted fixes

The release includes performance work (#340), Actions maintenance
(#385 through #389), package function lookup (#396), the larger serialized
data cap (#393), `.data` inference (#394), the mirai contract (#399), opaque
class metadata (#395), forwarded defaults (#400), rejected default modes
(#397), and condition coercion (#398).

Release qualification also found a cyclic-data crash in `workflowsets`
(#401) and a false condition-length warning in `rstan` (#402). Both have
small regression fixtures. The decoder fix preserves the unrelated
unbound-name diagnostic; the subset fix preserves checks on index expressions.

```r
choices <- c(if (flag) "a", "b", "c")
if (choices[1L] == "a") 1L
```

A scalar subscript selects one element here. An opaque receiver cannot
justify carrying the source length into the result.

## Investigation outcomes and remaining work

Published 0.9.0 and the tracked-source baseline at `dde15b6` reproduce the
package reports below with the same inputs. The investigation used R 4.6.1,
embedded stubs, no user stub overrides, and explicit installed-library
controls. These are bounded reproduction results, not a replay of the
historical 472-package audit.

| Issue | Evidence | Disposition |
| :-- | :-- | :-- |
| #363 | dtplyr `dbe32a6` excludes `vignettes/benchmark.R` in `.Rbuildignore`. Direct checking reports three parse errors. Removing that exclusion in a copy changes discovery from 71 to 72 files. | Discovery follows the documented exclusion. Explain skipped files and reinclusion in the infrastructure release. |
| #366 | param6 0.2.4 and distr6 1.6.9 produce 133 and 189 RY010 findings without dependency inventories. Static set6/dictionar6 export inventories make param6 clean. | Improve inventory availability. A blanket unknown-import fallback also hides unrelated typos. |
| #378 | gt 1.3.0 has 8,046,404 decoded bytes in `R/sysdata.rda`, above the old 2 MiB cap. | The 16 MiB default resolves its internal data names. Oversized files still produce a notice. |
| #379 | All 34 otelsdk 0.2.4 findings pass native symbols through a cleancall wrapper. The direct `.Call` symbol is already recognized. | Model wrapper and registration contracts in the infrastructure release. |
| #383 | The `.data` failure comes from inference when a data-mask helper has no usable data argument. | Keep the pronoun opaque. The reproduced failure did not establish artifact drift. |
| #372 | Bare package-mode scalar guards warn; qualified base calls avoid the warning. Imported masks and S3 length methods prevent a general scalar proof. | Add inventory completeness and parameter class provenance in the infrastructure release. |
| #381 | The bounded fix skips known non-functions and corrects package/dataset precedence. Tests pin the remaining S7 and outer-frame cases. | Complete frame identity and lookup provenance in the infrastructure release. |

These open boundaries remain in the
[infrastructure milestone](https://github.com/sims1253/ry/milestone/2).
They are not claims of completed bug fixes. New platforms (#391), the parser
and hashing dependency migration (#390), and new diagnostic families remain
outside 0.9.1.

## Corpus acceptance

The committed Posit ledger contains 443 records: 37 true positives and 406
false positives. The tidyverse ledger contains 103 records: 10 true positives,
41 false positives, and 52 without an owner. Both full native reconciliation
checks passed on the candidate, as did both report/identity drift controls.
The Posit records contain 441 distinct readable identities.

Earlier labels were corrected with runtime evidence: function-mode lookup
invalidated two old shadowing rationales, and four renv conditions accept
valid scalar flags. The ledger notes preserve those audits. A lower count
alone is not evidence that true positives survived.

The broad comparison completed all 472 frozen current package trees without
a crash or timeout. Findings fell from 3,458 with published 0.9.0 to 3,122
with the candidate: 337 removals and one addition, a net reduction of 9.7%.
The original audit did not record source hashes, so this comparison does
not replay its historical 2,852 findings.

| Rule | Removed sites | Main change |
| :-- | --: | :-- |
| RY010 | 159 | Larger serialized-data limit |
| RY070 | 124 | Function-mode lookup |
| RY061 | 39 | Opaque `.data`, mirai contract, and default/branch inference |
| RY001 | 12 | Condition coercion and uncertain lengths |
| RY092 | 3 | Opaque class metadata |

The added site is RY032 in `modelbased/R/utils.R:182:5`. Its existing
nullable-parameter heuristic warns even though scalar/default inputs run
without an error. A vector-valued `modelbased_integer` option does produce
the length-two error in R 4.6.1. The finding therefore remains a heuristic
warning, not a newly proved package bug. [The follow-up in #351](https://github.com/sims1253/ry/issues/351#issuecomment-5608873722)
records both controls. Another 181 retained findings changed wording only:
140 RY001 and 41 RY070 messages.

The [qualification record](0.9.1-qualification.json) preserves package tree
hashes, result hashes, every diagnostic change, per-rule counts, and the
source/binary identity. It records a tracked-source build of
`f6c229ef7d865c798b327799fb02d8ca961e4166`; later changes in the preparation
PR only record these results and clarify the extension changelog.

The source snapshot's manifest SHA-256 is
`353f06c49a86a9ba95850f9c179022f4b98a24afaef4fed4cea044dff88e988c`.
Its file-manifest SHA-256 is
`ceb85a629d1454338b05d9ac7e1bf277190c117ce32a1dfa658d2382f0557547`.
All 128,789 source entries (128,759 files and 30 symlinks) were verified
before the comparison. Published
0.9.0 receipts were reused after checking their binary, stdout, and stderr
hashes against the same frozen inputs and command. The candidate ran afresh.

## Artifact size and runtime acceptance

[Dry run 34404425926](https://github.com/sims1253/ry/actions/runs/34404425926)
built all six targets from `f6c229e` with cargo-dist 0.32.0 and the `dist`
profile. Every native smoke job passed. Both publication jobs were skipped.
Downloaded archives and executable sidecars were checked again locally;
their sizes and hashes match the native-runner records.

Sizes below are bytes, measured against the published 0.9.0 artifacts.
Downloads and installed executables are separate measurements. These
release-to-candidate differences include all intervening source changes;
they do not isolate the effect of #340.

| Target | 0.9.0 download | Candidate download | Change | 0.9.0 installed | Candidate installed | Change |
| :-- | --: | --: | --: | --: | --: | --: |
| Linux x86_64 | 4,144,471 | 3,754,624 | -9.4% | 12,836,104 | 8,935,944 | -30.4% |
| Linux aarch64 | 3,996,511 | 3,523,355 | -11.8% | 12,104,272 | 7,772,448 | -35.8% |
| macOS x86_64 | 4,010,561 | 3,649,000 | -9.0% | 12,022,560 | 8,344,528 | -30.6% |
| macOS aarch64 | 3,796,279 | 3,388,914 | -10.7% | 11,204,208 | 7,487,536 | -33.2% |
| Windows x86_64 | 4,124,400 | 4,053,988 | -1.7% | 12,592,128 | 10,627,584 | -15.6% |
| Windows aarch64 | 3,960,464 | 3,882,940 | -2.0% | 11,334,656 | 9,391,104 | -17.1% |

The native smoke artifacts now report both sizes on every dry run and
release. The qualification JSON also retains each build's toolchain and
environment metadata. This completes #337's platform and reporting
acceptance. No size budget or additional dependency reduction is introduced.

On Linux x86_64, 40 alternating paired samples after three warmups gave
these median wall times for published 0.9.0 and the candidate distribution
binary. CLI diagnostic output matched on all three checking workloads.

| Workload | 0.9.0 | Candidate |
| :-- | --: | --: |
| `ry version` | 1.31 ms | 1.19 ms |
| Two-line scalar arithmetic | 8.39 ms | 8.14 ms |
| Small file using dplyr | 12.92 ms | 8.95 ms |
| 200 small function files | 23.77 ms | 20.74 ms |

These are warm-cache measurements on one machine, including process
startup, not platform-wide speed guarantees. The qualification JSON keeps
the inputs and samples. [PR #340](https://github.com/sims1253/ry/pull/340)
contains the larger workload measurements and rejected optimization
tradeoffs. The integrated candidate retains the multi-thread LSP runtime.

## Release checks

Follow [the release runbook](../release-runbook.md). The checksum hook now
runs each verified archive on a native runner, checks the version and an
RY040 error, then repairs the file and requires a clean check. It records
compressed and installed size separately. All six smoke jobs must pass
before the host job can publish.

Candidate workspace tests, the full R oracle/semantic-list matrix, Clippy,
formatting, and the tracked-only locked release build passed. The parser
fuzz run completed 152,286 inputs in 301 seconds without a crash. The
1,000-seed LSP session test passed in 675.46 seconds.

Installed VSIX tests passed in VS Code 1.137.0 on Linux x86_64 with fresh
trusted and untrusted profiles. They used the tracked-source 0.9.1 binary
and covered diagnostics, server replacement/restart, failed replacement
recovery, and live rule settings. The downloaded Linux distribution binary
separately passed CLI/LSP error parity, diagnostic removal after repair,
and diagnostic restoration after restarting the server.

The release tracker records the final merge and CI state. The maintainer
will tag and publish later, then perform the runbook's marketplace,
Positron, and Zed installation checks against the published products.
No core or editor release was published during this preparation.
