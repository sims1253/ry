[package]
name = "ry-analysis"
description = "Unified analysis host for ry — one semantic snapshot for all callers."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
ry-core = { path = "../ry-core" }
ry-checker = { path = "../ry-checker" }
ry-config = { path = "../ry-config" }
ry-workspace = { path = "../ry-workspace" }
ry-typeshed = { path = "../ry-typeshed" }
tracing = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
